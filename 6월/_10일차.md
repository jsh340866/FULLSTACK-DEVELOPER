# 2026-06-15 - REST API / 외부 API 연동

## 주제
C06 PortOne 결제 API 연동

## 오늘자 주요 태그

`#PortOne`
`#RESTAPI`
`#RestTemplate`
`#HttpHeaders`
`#HttpEntity`
`#MultiValueMap`
`#JSONObject`
`#ResponseEntity`
`#Authorization`
`#BearerToken`

## 오늘 배운 것

이번 챕터는 지금까지 배운 REST API 개념을 실제 결제 서비스에 적용하는 과정입니다.

이전까지는

```text
공공데이터 API
OpenDart API
```

를 호출했다면,

이번에는 실제 결제 서비스인 PortOne API와 통신합니다.

핵심은

```text
토큰 발급

↓

Authorization 인증

↓

본인인증 조회

↓

결제 조회

↓

결제 목록 조회

↓

결제 취소
```

입니다.

---

## 1. PortOneController 구조 (`C06Portone/PortoneController.java`)

### 설명

이번 컨트롤러는 PortOne API를 호출하기 위한 테스트용 컨트롤러입니다.

모든 API 호출은

```java
HOST = "https://api.iamport.kr"
```

를 기준으로 수행됩니다.

### 실제 코드

```java
@Controller
@Slf4j
@RequestMapping("/Portone")
public class PortoneController {
```

```java
String HOST = "https://api.iamport.kr";
```

```java
String IMP_KEY = "...";
String IMP_SECRET = "...";
```

### 코드 설명

#### @Controller

```java
@Controller
```

Spring MVC 컨트롤러 등록

---

#### @RequestMapping

```java
@RequestMapping("/Portone")
```

모든 요청의 공통 URL

예)

```text
/Portone/index
/Portone/payments
/Portone/certifications
```

---

#### HOST

```java
String HOST = "https://api.iamport.kr";
```

PortOne 서버 주소

이후 모든 API 요청 주소 생성에 사용됩니다.

---

### 핵심 정리

```java
HOST
```

↓

PortOne 서버 주소

```java
IMP_KEY
IMP_SECRET
```

↓

API 인증 정보

---

## 2. Access Token 발급 (`getToken()`)

### 설명

PortOne API는 인증 없이 사용할 수 없습니다.

따라서 가장 먼저 Access Token을 발급받아야 합니다.

이 메서드는 이후

```java
certification()
payments()
payments_cancel()
```

등 모든 API 호출 전에 실행됩니다.

### 실제 코드

```java
private void getToken(){
```

```java
String url = HOST + "/users/getToken";
```

```java
MultiValueMap<String,String> params =
        new LinkedMultiValueMap<>();

params.add("imp_key",IMP_KEY);
params.add("imp_secret",IMP_SECRET);
```

```java
HttpEntity<
        MultiValueMap<String,String>
        > entity =
        new HttpEntity<>(params,header);
```

```java
ResponseEntity<PortOneTokenResponse>
        response =
        restTemplate.exchange(
                url,
                HttpMethod.POST,
                entity,
                PortOneTokenResponse.class
        );
```

### 코드 설명

#### URL 생성

```java
String url =
        HOST + "/users/getToken";
```

최종 요청

```text
https://api.iamport.kr/users/getToken
```

---

#### MultiValueMap

```java
MultiValueMap<String,String>
```

Form 데이터 저장 객체

---

```java
params.add("imp_key",IMP_KEY);
```

REST API KEY 저장

---

```java
params.add("imp_secret",IMP_SECRET);
```

REST API SECRET 저장

---

실제 전송 데이터

```text
imp_key=xxxxx
imp_secret=yyyyy
```

---

#### HttpEntity

```java
new HttpEntity<>(params,header)
```

Header + Body 묶음

---

#### exchange()

```java
restTemplate.exchange(...)
```

외부 API 호출

---

#### 응답 저장

```java
this.portOneTokenResponse =
        response.getBody();
```

발급받은 토큰 저장

---

### 예상 응답

```json
{
  "code":0,
  "response":{
    "access_token":"eyJ..."
  }
}
```

---

### 핵심 정리

```java
MultiValueMap
```

↓

토큰 요청 데이터 생성

```java
exchange()
```

↓

토큰 발급 요청

```java
portOneTokenResponse
```

↓

토큰 저장

---

## 3. 본인인증 조회 (`certification()`)

### 설명

PortOne 본인인증 결과 조회 API

사용자의

```text
이름
생년월일
전화번호
성별
```

등을 확인할 수 있습니다.

### 실제 코드

```java
@GetMapping("/certifications/{imp_uid}")
```

```java
String url =
        HOST + "/certifications/"
                + imp_uid;
```

```java
header.add(
        "Authorization",
        "Bearer "
        + portOneTokenResponse
            .getResponse()
            .getAccess_token()
);
```

```java
ResponseEntity<
        CertificationResponse
        > response =
        restTemplate.exchange(
                url,
                HttpMethod.GET,
                entity,
                CertificationResponse.class
        );
```

### 코드 설명

#### imp_uid

```java
@PathVariable String imp_uid
```

PortOne 본인인증 고유번호

---

#### Authorization

```java
Authorization:
Bearer access_token
```

토큰 인증

---

#### GET 요청

```java
HttpMethod.GET
```

조회 목적

---

#### DTO 매핑

```java
CertificationResponse.class
```

응답 JSON → 객체 변환

---

### 핵심 정리

```java
imp_uid
```

↓

본인인증 조회

```java
Authorization
```

↓

사용자 인증

```java
CertificationResponse
```

↓

JSON 자동 매핑

---

## 4. 결제 조회 (`payments()`)

### 설명

merchant_uid를 이용해 결제 정보를 조회합니다.

실무에서는

```text
결제 검증

결제 상태 확인

금액 검증
```

등에 사용됩니다.

### 실제 코드

```java
@GetMapping("/payments/{merchant_uid}")
```

```java
String url =
HOST
+ "/payments?imp_uid[]=&merchant_uid="
+ merchant_uid;
```

```java
ResponseEntity<String>
        response =
        restTemplate.exchange(
                url,
                HttpMethod.GET,
                entity,
                String.class
        );
```

### 코드 설명

#### merchant_uid

```java
merchant_uid
```

주문번호

---

예)

```text
ORDER_1001
```

---

#### URL 생성

```java
/payments
```

조회 API 호출

---

#### String.class

```java
String.class
```

응답을 문자열 그대로 받음

---

### 핵심 정리

```java
merchant_uid
```

↓

결제 조회

```java
exchange(GET)
```

↓

결제정보 요청

---

## 5. 결제 상태별 조회 (`payments_all()`)

### 설명

결제 상태별 목록 조회

예)

```text
paid

ready

cancelled

failed
```

---

### 실제 코드

```java
@GetMapping(
"/payments/status/{payment_status}"
)
```

```java
params.add("page","1");
params.add("limit","20");
```

```java
String url =
HOST
+ "/payments/status/"
+ payment_status;
```

### 코드 설명

#### page

```java
page=1
```

1페이지 조회

---

#### limit

```java
limit=20
```

20건 조회

---

#### payment_status

```java
paid
```

결제완료

---

```java
cancelled
```

결제취소

---

### 핵심 정리

```java
page
```

↓

페이지 번호

```java
limit
```

↓

조회 건수

```java
payment_status
```

↓

상태 필터

---

## 6. 결제 취소 (`payments_cancel()`)

### 설명

이미 승인된 결제를 취소합니다.

이번 챕터에서 가장 중요한 API입니다.

### 실제 코드

```java
String url =
        HOST + "/payments/cancel";
```

```java
JSONObject params =
        new JSONObject();
```

```java
params.put(
        "merchant_uid",
        "test_merchand1781501540604"
);
```

```java
HttpEntity<JSONObject>
        entity =
        new HttpEntity<>(
                params,
                header
        );
```

```java
ResponseEntity<String>
        response =
        restTemplate.exchange(
                url,
                HttpMethod.POST,
                entity,
                String.class
        );
```

### 코드 설명

#### JSONObject

```java
JSONObject
```

JSON 생성 객체

---

#### put()

```java
params.put(
        "merchant_uid",
        값
);
```

취소할 주문번호 저장

---

생성 JSON

```json
{
  "merchant_uid":"test123"
}
```

---

#### POST 요청

```java
HttpMethod.POST
```

결제 취소 요청

---

### 예상 응답

```json
{
  "status":"cancelled"
}
```

---

### 핵심 정리

```java
JSONObject
```

↓

JSON 생성

```java
merchant_uid
```

↓

취소 대상 지정

```java
POST
```

↓

결제 취소 요청

---

## 7. DTO 매핑 구조

### 설명

이번 예제는 JSON 응답을 DTO로 변환하는 구조를 학습하기 위한 예제이기도 합니다.

### 실제 코드

```java
@Data
private static class TokenResponse{
    public String access_token;
    public int now;
    public int expired_at;
}
```

```java
@Data
private static class PortOneTokenResponse{
    public int code;

    @JsonProperty("response")
    public TokenResponse response;
}
```

### 코드 설명

응답 JSON

```json
{
  "code":0,
  "response":{
    "access_token":"..."
  }
}
```

↓

자동 변환

```java
portOneTokenResponse
        .getResponse()
        .getAccess_token()
```

---

### 핵심 정리

```java
JSON
```

↓

DTO

↓

Java 객체

자동 변환

---

## 오늘 핵심 정리

이번 챕터는 지금까지 배운

```text
REST API

RestTemplate

ResponseEntity

DTO 매핑

JSON 처리
```

를 실제 결제 서비스에 적용하는 과정입니다.

핵심 기술은

```java
MultiValueMap
```

요청 데이터 생성

```java
HttpHeaders
```

헤더 설정

```java
HttpEntity
```

요청 객체 생성

```java
RestTemplate
```

외부 API 호출

```java
JSONObject
```

JSON 생성

```java
DTO
```

응답 매핑

입니다.

## 한 줄 요약

> C06은 PortOne 결제 API를 이용하여 Access Token 발급, 본인인증 조회, 결제 조회, 결제 상태 조회, 결제 취소를 구현하면서 RestTemplate·HttpEntity·JSONObject·DTO 매핑 구조를 실습하는 챕터입니다.
