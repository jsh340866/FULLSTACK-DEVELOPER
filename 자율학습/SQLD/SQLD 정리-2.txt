=============================================================================


2. 다음 중 인덱스를 사용할 수 없는 조건에 해당되지 않는 것은?

① 인덱스가 사용되는 칼럼에 NVL(Key, 0)을 사용했다.
② 인덱스가 사용되는 칼럼에 to_char(vin tagedate, 'yyyymmdd') = sysdate를 사용했다.
③ 인덱스가 사용되는 칼럼에 vintageyear = to_char(sysdate, 'yyyy')를 사용했다.
④ 인덱스가 사용되는 칼럼에 name||' ' = 'lim'을 사용했다.

###
정답 : 3

💡 핵심 콕 집어 명쾌한 해설
문제가 "인덱스를 사용할 수 없는 조건에 해당되지 않는 것"이라고 꼬아서 나왔지만, 결국 "인덱스를 정상적으로 사용할 수 있는(인덱스가 타는) 식은 무엇인가?"를 찾는 문제예요.

인덱스를 사용할 수 있느냐 없느냐는 "인덱스가 걸린 컬럼(좌변)을 가공했는가?"만 보시면 돼요!

1. 좌변을 변형하면 인덱스를 못 써요! (①, ②, ④)
조건절의 왼쪽(컬럼 이름이 있는 곳)에 함수를 씌우거나 연산자를 붙여서 값을 변형하면, 데이터베이스는 미리 만들어둔 인덱스를 활용하지 못하고 테이블 전체를 뒤져야 해요.
- ①번: Key 컬럼에 NVL 함수를 씌워서 가공함 ❌
- ②번: vin tagedate 컬럼에 to_char 함수를 씌워서 가공함 ❌
- ④번: name 컬럼에 || (문자열 연결 연산)을 해서 가공함 ❌

2. 우변은 아무리 가공해도 인덱스를 쓸 수 있어요! (③)
- ③번: vintageyear = to_char(sysdate, 'yyyy')
	- 왼쪽 컬럼(vintageyear)은 아무런 가공 없이 깨끗한 상태 그대로 있죠?
	- 오른쪽의 비교하는 값(sysdate)에 함수를 쓰는 건 인덱스 작동에 아무런 방해가 되지 않아요.

📌 한 줄 요약
- WHERE 함수(컬럼) = 값 ➡️ 인덱스 사용 불가능
- WHERE 컬럼 = 함수(값) ➡️ 인덱스 사용 가능! (정답 ③번)


=============================================================================

27. 다음 중 설계 단계에서 데이터 모델의 성능을 고려하는 절차와 방법에 포함되지 않는 것은?

① 이력모델의 조정, 기본키/외래키 조정, 슈퍼타입/서브타입 조정 등을 수행한다.
② 데이터베이스 용량 산정을 수행한다.
③ 데이터베이스에 발생되는 트랜잭션의 유형을 파악한다.
④ 데이터 모델링을 할 때 분석 단계에서부터 반정규화를 수행한다.

###
정답 : ④

🔍 왜 ④번이 정답(포함되지 않는 것)일까요?
- 반정규화는 가장 마지막 보루입니다! 분석 단계부터 막 지르는 게 아니에요.
- 데이터 모델의 성능을 향상시키기 위해 구조를 쪼개고 정돈하는 '정규화'가 무조건 첫 번째 단계입니다. 정규화를 완벽히 끝내놓고 성능을 검토한 뒤, "아, 이 부분은 도저히 속도가 안 나오네?" 싶을 때 마지막 단계에서 성능 향상을 위해 테이블을 합치거나 중복을 허용하는 반정규화(역정규화)를 고민하는 것입니다.
- 지문처럼 "분석 단계에서부터 반정규화를 수행한다"는 말은 순서가 완전히 꼬인 잘못된 방법이기 때문에 확실한 오답입니다

📌 성능 데이터 모델링의 올바른 6단계 절차 (이것만 기억하세요!)
시험 문제에는 이 절차의 순서를 섞어놓거나 4번처럼 뜬금없는 시점에 뭘 한다고 속이는 유형이 많아요.
- 1단계: 데이터 모델링을 할 때 정규화를 정확하게 수행한다. (무조건 정규화가 1등!)
- 2단계: 데이터베이스 용량 산정을 수행한다. (지문 ②번)
- 3단계: 데이터베이스에 발생하는 트랜잭션의 유형을 파악한다. (지문 ③번)
- 4단계: 용량과 트랜잭션 유형에 따라 반정규화를 수행한다. (구조적 접근)
- 5단계: 데이터 성능 향상을 위해 이력모델 조정, PK/FK 조정, 슈퍼/서브타입 조정을 한다. (지문 ①번)
- 6단계: 성능 관점에서 데이터 모델을 최종 검증한다.

❌ 나머지 보기들이 올바른 설명인 이유
①, ②, ③번은 위의 6단계 절차에 아주 예쁘게 잘 포함되어 있는 정석적인 절차와 방법들입니다.


=============================================================================

##NOT IN - NULL 함정 조심

WHERE A.COL NOT IN (2, NULL)

예를 들어:

A.COL = 5

라고 하면

5 <> 2      → TRUE
5 <> NULL   → UNKNOWN

최종:

TRUE AND UNKNOWN

결과:

UNKNOWN

WHERE 절은 TRUE만 통과시키므로 탈락.
=============================================================================

🎯 시험장 꿀팁 (암기 공식)! 관계를 구성하는 요소
정처기 시험에서 "관계를 구성하는 3대 요소가 아닌 것은?" 하고 물어보면 앞 글자만 따서 **[명. 차. 선]**을 기억하세요!

- 명: 관계명
- 차: 관계차수 (Cardinality)
- 선: 관계선택성 (Optionality)

=============================================================================


[문제] 제시된 계층형 질의 SQL의 실행 결과로 가장 적절한 것은?

--------------------------------------------------

SELECT 사원번호, 이름, LEVEL
FROM 사원
  START WITH 사원번호 IN (1006, 1001) 
CONNECT BY PRIOR 상위관리자코드 = 사원번호;

--------------------------------------------------

사원번호 | 이름   | 상위관리자코드
----------------------------------
1000     | 홍길동 | NULL
1001     | 박길동 | 1000
1002     | 최길동 | 1001
1003     | 이길동 | 1001
1004     | 구길동 | 1002
1005     | 안길동 | 1003
1006     | 송길동 | 1000
1007     | 강길동 | 1006
1008     | 공길동 | 1006

--------------------------------------------------
[정답 및 결과 값]
최종적으로 총 4개의 행이 아래와 같은 순서와 LEVEL로 조회됩니다.

사원번호 | 이름   | LEVEL
------------------------
1006     | 송길동 | 1
1001     | 박길동 | 1
1000     | 홍길동 | 2
1000     | 홍길동 | 2
(※ 주의: DBMS의 정렬 옵션에 따라 1006 계통과 1001 계통의 출력 순서는 바뀔 수 있습니다.)

[핵심 해설]
1. START WITH 사원번호 IN (1006, 1001)
   - 시작점(루트 노드)을 '송길동(1006)'과 '박길동(1001)' 두 군데로 잡습니다. 이들의 LEVEL은 1이 됩니다.

2. CONNECT BY PRIOR 상위관리자코드 = 사원번호 (★핵심 함정)
   - 'PRIOR(이전 행)의 상위관리자코드'가 '현재 행의 사원번호'와 같은 것을 찾아가라는 뜻입니다.
   - 즉, 부하 직원을 찾는 것이 아니라 나를 관리하는 **"상위 관리자(부모 노드)"를 향해 거꾸로 올라가는 '역방향 전개'**입니다.

3. 전개 과정 분석:
   - [송길동(1006) 계통]
     * 송길동(LEVEL 1)의 상위관리자코드는 '1000'입니다.
     * 사원번호가 '1000'인 사람을 찾으니 '홍길동(LEVEL 2)'이 나옵니다.
     * 홍길동(1000)의 상위관리자는 'NULL'이므로 여기서 전개가 종료됩니다.
   - [박길동(1001) 계통]
     * 박길동(LEVEL 1)의 상위관리자코드는 '1000'입니다.
     * 사원번호가 '1000'인 사람을 찾으니 '홍길동(LEVEL 2)'이 나옵니다.
     * 홍길동(1000)의 상위관리자는 'NULL'이므로 여기서 전개가 종료됩니다.

###
START WITH
= 시작 부모(ROOT, LEVEL1) 를 정하는 부분

CONNECT BY
= 부모(PRIOR)와 현재행의 연결조건을 정하는 부분




=============================================================================

[문제] 제시된 집합 연산 SQL의 실행 결과 행 수는?

지문

SELECT val
FROM (
SELECT 1 AS val
UNION ALL
SELECT 2
UNION ALL
SELECT 2
UNION ALL
SELECT 3
) a
MINUS
SELECT val
FROM (
SELECT 2 AS val
UNION
SELECT 4
) b;

--------------------------------------------------
[정답 및 결과 값]
2번 (2개 행)

[최종 출력 데이터 결과 테이블]
VAL
----
1
3

[핵심 해설]
1. 위쪽 서브쿼리 (a 집합) 계산: UNION ALL
   - UNION ALL은 중복을 제거하지 않고 모든 데이터를 그대로 합칩니다.
   - 데이터 내용: [1, 2, 2, 3] (총 4개 행)

2. 아래쪽 서브쿼리 (b 집합) 계산: UNION
   - 순수 UNION은 중복된 데이터를 자동으로 제거(정렬 후 중복 제거)합니다.
   - 데이터 내용: [2, 4] (총 2개 행)

3. 최종 연산: a 집합 MINUS b 집합 (차집합) ⭐⭐⭐
   - 오라클의 MINUS(차집합) 연산자는 아주 중요한 절대 규칙이 있습니다.
     "앞의 집합(a)에서 뒤의 집합(b)에 있는 원소를 빼버리는데, 
      그 과정에서 결과 집합의 중복을 '자동으로 제거'하고 최종 결과를 보여준다!"
   
   - 연산 과정:
     * a 집합 [1, 2, 2, 3] 에서 b 집합에 있는 '2'와 '4'를 지웁니다.
     * 내부적으로 중복을 제거, 사실상 [1, 2, 3] - [2, 4] 이다
     * 2가 지워지고 나면 [1, 3] 이 남습니다.
     * 결과 집합에 중복이 없으므로 그대로 [1, 3] 총 2개의 행이 최종 출력됩니다.


=============================================================================

### 💡 무조건 외우는 정규화 6단계 (도·부·이·결·다·조)

[정규화 단계 - 암기 키워드 - 대상 및 특징]

- 제1 정규형 (1NF) : [도]메인 원자값 / 도메인이 원자값이어야 함
- 제2 정규형 (2NF) : [부]분 함수 / 부분 함수적 종속 제거 (완전 함수 종속)
- 제3 정규형 (3NF) : [이]행적 / 이행적 함수적 종속 제거 (A→B, B→C 제거)
- BCNF             : [결]정자 / 결정자이면서 후보키가 아닌 것 제거
- 제4 정규형 (4NF) : [다]치 종속 / 다치 종속 제거
- 제5 정규형 (5NF) : [조]인 종속 / 조인 종속성 이용


=============================================================================

## 비율 함수

1️⃣ RATIO_TO_REPORT   (중요)
= 그룹 내 비율
👉 보면 바로 정답

2️⃣ CUME_DIST    (누적 비율)
= 누적 비율
👉 “이하 포함” 보이면 바로 찍기

3️⃣ PERCENT_RANK   (비율처럼 보이지만 아님)
= 순위 기반 위치
👉 “순위 / 상대 위치” 느낌이면 선택

4️⃣ (보너스) SUM + OVER (실질 비율 계산 기반)
SAL / SUM(SAL) OVER(PARTITION BY DEPTNO)
= 직접 비율 계산
✔ RATIO_TO_REPORT와 동일 결과


✔ 비율 = RATIO_TO_REPORT
✔ 직접 계산 = SUM OVER
✔ 헷갈리는 함정 = PERCENT_RANK
✔ 누적 = CUME_DIST

=============================================================================

아래 SQL 중 출력 결과가 다른 것은?

1️⃣ SELECT * FROM EMP WHERE (DEPTNO = 10 OR DEPTNO = 20) AND JOB = 'CLERK' OR SAL > 3000;
2️⃣ SELECT * FROM EMP WHERE (DEPTNO = 10 AND JOB = 'CLERK') OR (DEPTNO = 20 AND JOB = 'CLERK') OR SAL > 3000;
3️⃣ SELECT * FROM EMP WHERE DEPTNO = 10 OR DEPTNO = 20 AND JOB = 'CLERK' OR SAL > 3000;
4️⃣ SELECT * FROM EMP WHERE DEPTNO IN (10,20) AND JOB = 'CLERK' OR SAL > 3000;

정답 : 2 번


###
해설

🔎 핵심 개념: SQL 연산자 우선순위

SQL에서는 조건을 해석할 때 다음 우선순위를 가집니다.

AND > OR
괄호가 있으면 괄호가 최우선

즉, 괄호가 없으면 AND가 먼저 묶이고 OR이 나중에 처리됩니다.


1. (DEPTNO = 10 OR DEPTNO = 20) AND JOB = 'CLERK' OR SAL > 3000;
	- ((DEPTNO = 10 OR DEPTNO = 20) AND JOB = 'CLERK') OR SAL > 3000;

2. (DEPTNO = 10 AND JOB = 'CLERK') OR (DEPTNO = 20 AND JOB = 'CLERK') OR SAL > 3000;
	- ((DEPTNO = 10 OR DEPTNO = 20) AND JOB = 'CLERK') OR SAL > 3000; 변환 가능

3.  DEPTNO = 10 OR DEPTNO = 20 AND JOB = 'CLERK' OR SAL > 3000;
	- DEPTNO = 10 OR (DEPTNO = 20 AND JOB = 'CLERK') OR SAL > 3000;
	- OR에 괄호가 없어 AND와 묶여 다른 값이 된다

4. DEPTNO IN (10,20) AND JOB = 'CLERK' OR SAL > 3000;
	- ((DEPTNO = 10 OR DEPTNO = 20) AND JOB = 'CLERK') OR SAL > 3000; 변환 가능





=============================================================================

## 📝 [오답노트] 서브쿼리 연산자 에러 유형 (복수 정답 반영)

### 📌 문제
주어진 ERD를 참조하여 SQL을 수행하였을 때, 문법적 또는 논리적 오류가 발생하여 실행되지 않거나 에러(Error)가 날 수 있는 SQL문은? (정답: 1번, 2번, 4번)

1. SELECT 회원번호, 종목코드 FROM 일자별주문내역 WHERE 주문일자 ALL (SELECT DISTINCT 주문일자 FROM 계좌마스터);
2. SELECT 회원번호, 종목코드 FROM 일자별주문내역 WHERE 주문일자 EXISTS (SELECT DISTINCT 주문일자 FROM 계좌마스터);
3. SELECT * FROM 계좌마스터 WHERE 회원번호 IN (SELECT DISTINCT 회원번호 FROM 고객);
4. SELECT * FROM 계좌마스터 WHERE 회원번호 = (SELECT DISTINCT 회원번호 FROM 고객);

---

### 🔍 상세 해설

- 1번이 에러인 이유 (구문 오류) :
  * ALL 연산자는 단독으로 사용할 수 없고, 반드시 비교 연산자(=, >, <, >=, <=)와 함께 사용해야 함 (예: WHERE 주문일자 = ALL (...)). 
  * 비교 연산자 없이 '주문일자 ALL'이라고만 적었기 때문에 명백한 SQL 구문 에러(Syntax Error)가 발생함.

- 2번이 에러인 이유 (구문 오류) :
  * EXISTS 연산자는 서브쿼리의 결과가 존재하는지만 확인하는 연산자이므로, 앞에 컬럼명(주문일자)을 붙이지 않고 'WHERE EXISTS (...)' 형태로 사용해야 함.
  * 컬럼명 뒤에 EXISTS를 붙였기 때문에 이 역시 잘못된 문법으로 구문 에러가 발생함.

- 4번이 에러인 이유 (런타임 오류) :
  * 등호(=)는 오직 하나의 값만 비교하는 '단일 행 연산자'임. 하지만 서브쿼리(고객 테이블의 회원번호 조회)의 결과는 여러 개의 행(다중 행)을 반환함.
  * 단일 행 연산자 뒤에 여러 행이 들어오기 때문에 "단일 행 하위 질의에 2개 이상의 행이 반환되었습니다"라는 치명적인 에러가 발생함.
  * 등호(=)를 in으로 바꾸면 해결

- 3번만 정상 작동하는 이유 :
  * IN은 서브쿼리에서 반환된 여러 개의 값 중 하나라도 일치하는 게 있는지 검사하는 올바른 '다중 행 연산자'이며, 문법과 구조 모두 완벽하여 정상 실행됨.
  

---

### 💡 핵심 요약 키워드
- ALL / ANY : 반드시 앞에 비교 연산자(=, >, < 등)를 동반해야 함
- EXISTS : 앞에 컬럼명을 쓰지 않고 'WHERE EXISTS (서브쿼리)' 형태로만 사용함
- 단일 행 연산자(=) : 서브쿼리의 결과 행이 무조건 딱 1개여야 에러가 안 남

=============================================================================

## 📝 [오답노트] 셀프 조인(Self Join)과 아우터 조인(Outer Join) 활용

### 📌 문제
다음 출력 결과를 얻기 위한 SQL 문장으로 가장 적절한 것은?

[문제 이미지 데이터 텍스트화]
<EMP>
EMPNO | ENAME   | MGR  | DEPTNO
7369  | SMITH   | 7566 | 20
7499  | ALLEN   | 7654 | 30
7521  | WARD    | 7566 | 30
7566  | JONES   | 7654 | 20
7654  | MARTIN  |      | 30

<결과>
EMPNO | ENAME   | MANAGER_NAME
7369  | SMITH   | JONES
7499  | ALLEN   | MARTIN
7521  | WARD    | JONES
7566  | JONES   | MARTIN
7654  | MARTIN  | 

[SQL]
1. SELECT E1.EMPNO, E1.ENAME, E2.ENAME AS MANAGER_NAME FROM EMP E1, EMP E2 WHERE E1.EMPNO = E2.MGR(+) ORDER BY E1.EMPNO; (내 답안)
2. SELECT E1.EMPNO, E1.ENAME, E2.ENAME AS MANAGER_NAME FROM EMP E1, EMP E2 WHERE E1.EMPNO = E2.MGR ORDER BY E1.EMPNO;
3. SELECT E1.EMPNO, E1.ENAME, E2.ENAME AS MANAGER_NAME FROM EMP E1, EMP E2 WHERE E1.MGR = E2.EMPNO(+) ORDER BY E1.EMPNO; (정답)
4. SELECT E1.EMPNO, E1.ENAME, E2.ENAME AS MANAGER_NAME FROM EMP E1, EMP E2 WHERE E1.MGR = E2.EMPNO ORDER BY E1.EMPNO;

---

### 🔍 상세 해설

- **조인 조건 분석** : 
  * 사원 테이블(E1)의 관리자 사원번호(`MGR`)를 통해 관리자 테이블(E2)의 사원번호(`EMPNO`)를 찾아 이름(`ENAME`)을 매칭하는 셀프 조인 문제임.
  * 결과 테이블에서 매니저가 없는 'MARTIN' 사원도 누락되지 않고 출력되었으므로 `LEFT OUTER JOIN`이 필요함.

- **풀이/매칭 진행 과정** : 
  * Oracle 문법에서 Outer Join 기호인 `(+)`는 데이터가 존재하지 않아 NULL로 채워져야 하는 쪽에 붙임. 매니저 정보가 없는 사원도 살려야 하므로 `E2` 테이블 컬럼에 `(+)`를 붙여야 함.
  * [7369, SMITH] (MGR: 7566) -> E2에서 EMPNO 7566인 JONES 매칭 성공.
  * [7654, MARTIN] (MGR: NULL) -> E2에서 매칭되는 EMPNO 없음. 단, `E2.EMPNO(+)` 조건에 의해 탈락하지 않고 우측 데이터는 NULL 처리되어 정상 출력됨.

- **최종 결과 도출** : 
  * 사원의 관리자 번호로 관리자의 사원번호를 조인해야 하므로 기본 조건은 `E1.MGR = E2.EMPNO`가 됨.
  * 여기에 매니저가 없는 사원까지 누락 없이 표현하기 위해 오른쪽 테이블 컬럼 뒤에 `(+)`를 붙인 **3번**이 최종 정답임.

---

### 💡 핵심 요약 키워드
- **셀프 조인의 방향성** : 사원-관리자 관계 조인 시 기준점은 `사원.MGR = 관리자.EMPNO`가 됨.
- **오라클 Outer Join 기호 `(+)`** : LEFT OUTER JOIN 구현 시, 우측 테이블(NULL로 채워질 대상) 컬럼 뒤에 `(+)`를 추가함.

=============================================================================

⚠️ 집합 연산자(UNION ALL)의 절대 규칙
UNION이나 UNION ALL 같은 집합 연산자를 쓸 때는 위아래 SELECT 절이 다음 2가지 조건을 무조건 만족해야 합니다.

- 출력할 컬럼의 개수가 같아야 한다.
- 대응되는 컬럼의 데이터 타입(형식)이 서로 일치해야 한다.

=============================================================================


select case A.COL1
when null
then -1
else 0
end as C1
from A;

오류 ! when null 은  A.COL1 = null 라는 구문이됨 

###decode
DECODE( A.COL1 ,  NULL ,  -1 ,  A.COL1 )
A.col1이 null이면 -1 줘라
아니면 A.col1 그대로


=============================================================================

문제

다음 중 B-Tree 인덱스에 대한 설명으로 올바른 것은?
답안과 정답

1번 - 비트맵 인덱스
정보계와 같은 Data warehouse 에서 사용하고 많은 양의 데이터를 검색할 때 사용한다.

2번 - 비트맵 인덱스
인덱스 내부 구조에 해당 데이터가 있으면 1, 없으면 0으로 저장한다.

3번 - B-Tree 인덱스 (정답)
분기를 수행하는 브랜치 블록, 인덱스를 구성하는 칼럼값을 가지는 리프 블록으로 구성된다.

4번 - 클러스트형 인덱스
인덱스를 생성하면 물리적으로 정렬되기 때문에 빠르게 연속적인 데이터 블록을 검색할 수 있다.


=============================================================================

문제

데이터베이스를 정의하고 접근하기 위해서는 데이터베이스 관리 시스템과의 통신 수단이 필요한데 이를 데이터 언어(Data Language)라고 하며, 그 기능과 사용 목적에 따라 데이터 정의어(DDL), 데이터 조작어(DML), 데이터 제어어(DCL)로 구분된다. 다음 중 데이터 언어와 SQL 명령어에 대한 설명으로 가장 부적절한 것은?
답안과 정답

1번
내 답안
호스트 프로그램 속에 삽입되어 사용되는 DML 명령어들을 데이터 부속어 (Data Sub Language) 라고 한다.

2번
DDL은 스키마, 도메인, 테이블, 뷰, 인덱스를 정의하거나 변경 또는 제거할 때 사용되며 CREATE, ALTER, DROP, RENAME 등이 있다.

3번
DML은 데이터베이스 사용자가 응용 프로그램이나 질의어를 통하여 저장된 데이터베이스를 실질적으로 접근하는데 사용되며
SELECT, INSERT, DELETE, UPDATE 등이 있다.

4번 (정답) - 절차적 언어에 대한 설명
비절차적 데이터 조작어(DML)는 사용자가 무슨 데이터를 원하며, 어떻게 그것을 접근해야 되는지를 명세하는 언어이다.

###
비절차적 DML (우리가 쓰는 SQL): 사용자가 "무슨 데이터(What)"를 원하는지만 컴퓨터에 던져주면 끝입니다. "어떻게(How) 가져올지"는 데이터베이스 시스템(옵티마이저)이 알아서 처리하기 때문에 사용자는 알 필요가 없어요.


=============================================================================

# 🗂️ 데이터베이스 인덱스(Index) 핵심 종류 요약

## 1. 물리적 구조에 따른 분류

* B-Tree 인덱스 (Balanced Tree Index)
  - 특징: 가장 범용적이고 기본적으로 사용되는 균형 잡힌 트리 구조.
  - 장점: 일치하는 값(=)뿐만 아니라 부등호(<, >)를 사용하는 '범위 검색'에 매우 강함.

* 비트맵 인덱스 (Bitmap Index)
  - 특징: 데이터를 0과 1(비트)로 변환하여 인덱스를 구성.
  - 장점: 컬럼이 가진 값의 종류가 적을 때(예: 성별 남/여, 대구/서울 등) 성능 극대화.
  - 단점: 데이터 수정이 잦으면 성능이 떨어져 대용량 분석용(DW)에 주로 사용.

--------------------------------------------------

## 2. 데이터 정렬 및 저장 방식에 따른 분류

* 클러스터형 인덱스 (Clustered Index)
  - 특징: 인덱스의 리프 페이지가 곧 실제 데이터 페이지 (책 페이지 번호 그 자체).
  - 특징: 실제 데이터가 인덱스 순서대로 물리적으로 정렬되어 저장됨.
  - 개수: 테이블당 딱 1개만 생성 가능 (주로 기본키(PK)가 자동 지정됨).

* 비클러스터형 인덱스 (Non-Clustered / Secondary Index)
  - 특징: 실제 데이터는 두고, 주소록 역할을 하는 별도의 인덱스 페이지를 생성 (책 맨 뒤 '찾아보기').
  - 특징: 데이터 자체는 물리적으로 정렬되지 않음.
  - 개수: 테이블당 여러 개 생성 가능.

--------------------------------------------------

## 3. 컬럼 구성에 따른 분류

* 단일 컬럼 인덱스 (Single-Column Index)
  - 하나의 컬럼만 지정해서 만든 가장 단순한 인덱스.

* 결합 인덱스 (Composite / Concatenated Index)
  - 두 개 이상의 컬럼을 합쳐서 하나의 인덱스로 만든 것.
  - ⚠️ 주의점: 조건절(WHERE)에 반드시 '첫 번째로 지정한 컬럼'이 들어가야 인덱스가 작동함.

--------------------------------------------------

## 💡 시험장 3초 암기 가이드

| 인덱스 종류 | 핵심 키워드 | 한 줄 요약 |
| :--- | :--- | :--- |
| B-Tree | 기본값 / 범위 검색 | 일치 검색 + 범위 검색 둘 다 만능 |
| 비트맵 | 값의 종류가 적음 | 성별, 지역 등 도메인이 작을 때 유리 |
| 클러스터형 | 테이블당 1개 / 물리 정렬 | 책 번호 그 자체, 속도 가장 빠름 |
| 비클러스터형 | 테이블당 여러 개 / 별도 주소록 | 책 뒤의 찾아보기, 부담 없이 여러 개 생성 |
| 결합 인덱스 | 여러 컬럼 조합 | 첫 번째 컬럼이 조건절에 무조건 필수 |

=============================================================================