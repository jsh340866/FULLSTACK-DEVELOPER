CREATE TABLE `financial_statement` (
   `id` bigint NOT NULL AUTO_INCREMENT,
   `bsns_year` char(4) DEFAULT NULL,
   `fs_div` char(3) DEFAULT NULL,
   `net_income` bigint DEFAULT NULL,
   `operating_income` bigint DEFAULT NULL,
   `reprt_code` char(5) DEFAULT NULL,
   `revenue` bigint DEFAULT NULL,
   `total_assets` bigint DEFAULT NULL,
   `total_equity` bigint DEFAULT NULL,
   `total_liabilities` bigint DEFAULT NULL,
   `stock_code` char(6) DEFAULT NULL,
   PRIMARY KEY (`id`),
   UNIQUE KEY `UKmd79vbq54qy938fggasujnd5i` (`bsns_year`,`stock_code`,`reprt_code`,`fs_div`),
   KEY `FK8898a8pwg7fxyupvgxutvevuq` (`stock_code`),
   CONSTRAINT `FK8898a8pwg7fxyupvgxutvevuq` FOREIGN KEY (`stock_code`) REFERENCES `company` (`stock_code`)
 ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci


SHOW INDEX FROM financial_statement;
SHOW CREATE TABLE financial_statement;

Company에 updateCorpCls() 메서드 추가 논의
DartItem, DartResponse 패키지 domain.dart로 이동
각 컬럼의 unique = true 제거 @UniqueConstraint만
DB에 복합 UK는 이미 있으니 테이블 삭제 후 재시작하면 깔끔하게 복합 UK만 
# 첫 실행 시
spring.jpa.hibernate.ddl-auto=create
# 이후부터는
spring.jpa.hibernate.ddl-auto=update


완료된 것들

Repository 5개 새로 작성
Service 5개 새로 작성
Controller 4개 새로 작성
StockPriceXmlParser 수정
AsyncConfig 수정 (dartExecutor 추가)
RestTemplateConfig → AsyncConfig에 통합
FinancialStatement unique 문제 해결

### 얘기할거
- Company에 updateCorpCls() 메서드 추가 논의
	- // Company 엔티티 내부에 메서드 추가(실무에서는 주로 이방법 선호)
public void updateCorpCls(String corpCls) {
    this.corpCls = corpCls;
}
	- 혹은 @Data 사용


- DartItem, DartResponse 패키지 domain.dart로 이동
- 각 컬럼의 unique = true 제거 @UniqueConstraint만 DB에 복합 UK는 이미 있으니 테이블 삭제 후 재시작하면 깔끔하게 복합 UK만 
- # 첫 실행 시
spring.jpa.hibernate.ddl-auto=create
- # 이후부터는
spring.jpa.hibernate.ddl-auto=update
- DividendInfo 설계 의도 확인 
	- 보통주/우선주 둘 다 저장할 건지, 보통주만 저장할 건지 (둘 다면 PK를 복합키로 변경 필요) 
	- 현재 보통주 기준
